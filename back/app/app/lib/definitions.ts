export type User = {
  id: string,
  name: string,
  email: string,
  password: string
};

export type Tournaments = {
    id: string,
    name: string,
    image: string,
    status: number,
    date: string,
};
